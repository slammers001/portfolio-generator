{
  "name": "personal-website-generator",
  "version": "1.0.0",
  "description": "A CLI tool to generate personal websites",
  "main": "src/generator.ts",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/generator.js"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "fs-extra": "^11.2.0",
    "inquirer": "^9.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/inquirer": "^8.2.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
